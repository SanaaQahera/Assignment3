#include <iostream>
using namespace std;

int main() {
	
	int n,div,sum,i;
	cout <<"Enter a number\n";
	cin >>n;
	sum = 0;
	for(i=1;i<n;i++)
	{
		if (n%i==0)
		sum = sum+i;
	}
	if (n==sum)
	cout <<"Number is a perfect number";
	else
	cout <<"Number is not a perfect number";
	return 0;
}
